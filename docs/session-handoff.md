# Session handoff

**Check this file** at the start of a work session (or when you say “read the handoff file”) so context stays consistent with other project builds.

## Pending

- **Git / GitHub:** Use **`ravenskys` only** from this point on. In **Windows Credential Manager**, remove old `github.com` entries, then from the repo root run `git push -u origin main` so the latest commits land on the remote. (Pushes were returning **403** when Windows used another account—fix credentials first.)

## When you resume

- Walk through the [**README**](../README.md) and, if needed, [plan.md](plan.md).
- Add new items under **Pending** (or a dated log below) as you go so the handoff file stays the single “continue here” place.

---

*Update this file whenever you end a session or need the next person/agent to pick up where you left off.*
